https://pajalnya-artem-epelman.vercel.app/

Pajalnya_test: Управління Проєктами та Завданнями
Огляд Проєкту

Pajalnya_test – це односторінковий додаток, призначений для ефективного управління проєктами
та завданнями всередині них. Застосунок реалізує складні інтерактивні елементи,
зокрема інтерактивні таблиці (сортування, фільтрація, зміна розміру колонок) та повноцінний
Kanban-board з Drag-and-Drop функціоналом для зміни порядку та статусу завдань.

Проєкт розроблений з використанням сучасного фронтенд-стеку з акцентом на типізацію та чисту
архітектуру стану.

🛠️ Використані Технології
Фреймворк: Vue.js 3 (Composition API).

Мова: TypeScript (Строга типізація).

Менеджер стану: Pinia.

HTTP-клієнт: Axios (для запитів до мокового API).

UI Фреймворк: PrimeVue (Використовується для таблиць, форм та інших UI-елементів).

Стилізація: SCSS (Використовується вперше, перехід від Tailwind).

D&D: VueDraggable, за рекомандацією з ТЗ (для Kanban та таблиць).

Сховище: LocalStorage (Збереження налаштувань користувача та стану).

Основний Функціонал
1. Управління Проєктами
   Головна сторінка відображає список проєктів і включає:

Інтерактивна Таблиця:

Стовпці: ID, Назва, Кількість завдань, Статус, Дата створення.

Можливості: Сортування (за ID, Назвою, Статусом), Фільтрація (за назвою та статусом),
Зміна ширини колонок.

CRUD-операції: Перегляд, Створення, редагування та видалення.

Форма: Модальне вікно для створення/редагування проєкту з валідацією.

2. Управління Завданнями (Сторінка Проєкту)
   На сторінці конкретного проєкту доступні два синхронізованих режими відображення завдань.
3. Обраний режим зберігається у LocalStorage.

A. Режим Таблиці
Колонки: ID, Назва, Виконавець, Статус, Термін виконання.

Можливості: Сортування (за Терміном виконання, Статусом), Фільтрація (за Виконавцем, Статусом).

Drag-and-Drop: Можливість зміни порядку рядків (завдань) всередині таблиці.

B. Режим Канбан-дошки
Колонки-статуси: "To do", "In Progress", "Done".

Повне Drag-and-Drop:

Зміна статусу: Перетягування картки між колонками.

Зміна порядку: Перетягування картки всередині колонки.

C. Форма Завдання
Поля: Назва завдання, Виконавець, Статус, Термін виконання.

Валідація: Реалізована валідація для всіх обов'язкових полів.

Робота з Даними та Станом (Pinia)
API та Обмеження
Проєкт використовує моковий API для симуляції роботи бекенда.

Обмеження Мокового API: Через клієнтську природу мокового API, серверна фільтрація,
пагінація та сортування відсутні. Всі ці операції виконуються на клієнті, що у великих проєктах
вимагало б переходу на повноцінний бекенд для підвищення швидкості завантаження сторінок.

Менеджмент Стану
Pinia Store: Дані розділено на модулі (projects-store та tasks-store) зі строгою типізацією.

Синхронізація: Використовується LocalStorage для збереження стану налаштувань
(фільтри, режим відображення).

Повідомлення: Реалізовані toast-повідомлення для підтвердження CRUD-операцій.

Інструкція з Розгортання та Запуску
Розгорнута Версія
Посилання на розгорнуту версію застосунку

Локальний Запуск
Для локального запуску вам знадобиться Node.js та npm/yarn.

Клонуйте репозиторій:

Bash

git clone
cd pajalnya_test
Встановіть залежності:

Bash

npm install
# або yarn install
Запустіть застосунок у режимі розробки:

Bash

npm run dev
# або yarn dev
